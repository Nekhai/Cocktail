import React from "react";
import { Router, Route } from "react-router-dom";

export const Content = () => {
  return (
    <div>
      <Router>
        <Route path="/" />
      </Router>
    </div>
  );
};
